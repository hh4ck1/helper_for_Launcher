{
  waila: {
    // Отображать блоки
    // Стандартное значение: [true]
    show_blocks: true,
    // Отображать жидкости
    // Попытается отображать жидкости
    // Стандартное значение: [false]
    show_fluids: false,
    // Отображать сущности
    // Стандартное значение: [true]
    show_entities: true,
    // Отображать иконки
    // Стандартное значение: [true]
    show_icon: true,
    // Положение иконки
    // Стандартное значение: [MIDDLE]
    // Доступные значения: [TOP, MIDDLE, BOTTOM]
    icon_position: "MIDDLE",
    // Отображать названия модов
    // Стандартное значение: [true]
    show_mod_name: true,
    // Отображать названия мода для предеметов
    // Стандартное значение: [true]
    show_item_mod_name: true,
    // Отображать названия из регистра
    // Стандартное значение: [false]
    show_registry: false
  },
  minecraft: {
    // Отображать позицию блока
    // Стандартное значение: [false]
    "block.position": false,
    // Отображать состояние блока
    // Стандартное значение: [false]
    "block.state": false,
    // Отображать сущности предметов
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "entity.item_entity": true,
    // Отображать позицию сущности
    // Стандартное значение: [false]
    "entity.position": false,
    // Отображать очки жизни
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "entity.health": true,
    // Отображать очки поглощения
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "entity.absorption": true,
    // Отображать очки брони
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "entity.armor": true,
    // Компактный режим
    // Отображать очки жизней и брони цифрами а не иконками
    // Стандартное значение: [false]
    "entity.compact": false,
    // Максимум инокок на строку
    // Количество иконок жизни и брони в строке перед началом следующей
    // Стандартное значение: [25]
    "entity.icon_per_line": 25,
    // Максимум очков жизни для отображения
    // Наибольшее число очков жизни для отображения в виде иконок, если у моба или игрока больше чем указано здесь, будут отображены цифры
    // Стандартное значение: [100]
    "entity.long_health_max": 100,
    // Максимум очков брони для отображения
    // Наибольшее число очков брони для отображения в виде иконок, если у моба или игрока больше чем указано здесь, будут отображены цифры
    // Стандартное значение: [100]
    "entity.long_armor_max": 100,
    // Отображать владельца
    // Только для лицензионных аккаунтов Minecraft
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "pet.owner": true,
    // Скрыть если владелец неизвестен
    // Стандартное значение: [false]
    "pet.hide_unknown_owner": false,
    // Отображать силу прыжка лошади
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "horse.jump_height": true,
    // Отображать скорость лошади
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "horse.speed": true,
    // Показывать характер панд
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "panda.genes": true,
    // Отображать координаты улья
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "bee.hive_pos": true,
    // Отображать уровень мёда
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "bee.hive_honey_level": true,
    // Отображать количество пчел в улье и пасеке
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "bee.hive_occupants": true,
    // Показать эффекты маяков
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "effect.beacon": true,
    // Показать эффекты мобов
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "effect.mob": true,
    // Показать скрытые эффекты мобов
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [false]
    "effect.hidden_mob": false,
    // Отображать играющую пластинку
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "jukebox.record": true,
    // Отображать таймер роста мобов
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "timer.grow": true,
    // Отображать таймер размножения
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "timer.breed": true,
    // Скрывать невидимые сущности
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "override.invisible_entity": true,
    // Отображать сундук-ловушку как обычный сундук
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "override.trapped_chest": true,
    // Отображать рыхлый снег как обычный
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "override.powder_snow": true,
    // Отображать заражённые блоки как обычные
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "override.infested": true,
    // Скрыть транспорт
    // Скрывать вагонетки, лошадей и прочие сущности, если игрок сидит на них
    // Стандартное значение: [true]
    "override.vehicle": true,
    // Показывать прогресс ломания
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "breaking_progress.enabled": true,
    // Цвет индикатора ломания
    // Стандартное значение: [-1426063361]
    "breaking_progress.color": -1426063361,
    // Только снизу
    // Стандартное значение: [false]
    "breaking_progress.bottom_only": false,
    // Отображать тип спаунера
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "spawner.type": true,
    // Отображать зрелость урожая
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "plant.crop_progress": true,
    // Отображать возможность роста культур
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "plant.crop_growable": true,
    // Отображать возможность роста деревьев
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "plant.tree_growable": true,
    // Отображать положение рычага
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "redstone.lever": true,
    // Отображать задержку повторителя
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "redstone.repeater": true,
    // Отображать режим компаратора
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "redstone.comparator": true,
    // Отображать уровень сигнала
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "redstone.level": true,
    // Отображать уровень компоста
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "level.composter": true,
    // Отображать инструмент и ноту
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    "note_block.type": true,
    // Режим отображения нот
    // Стандартное значение: [SHARP]
    // Доступные значения: [SHARP, FLAT]
    "note_block.note": "SHARP",
    // Число
    // Стандартное значение: [false]
    "note_block.int_value": false,
    // Показывать число книг на полке
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "book.bookshelf": true,
    // Показать книги на столе библиотекаря
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "book.lectern": true,
    // Отображение зачарований
    // Стандартное значение: [CYCLE]
    // Доступные значения: [SEPARATE, COMBINED, CYCLE, DISABLED]
    "book.enchantment": "CYCLE",
    // Время цикла (в миллисекундах)
    // Стандартное значение: [500]
    "book.enchantment_cycle_timing": 500,
    // Информация о книге
    // Стандартное значение: [true]
    "book.details": true
  },
  harvest: {
    // Отображать информацию для добычи
    // Это значение будет заменено значением с сервера
    // Стандартное значение: [true]
    enabled: true,
    // Режим отображения
    // Стандартное значение: [MODERN]
    // Доступные значения: [MODERN, CLASSIC, CLASSIC_MINIMAL]
    display_mode: "MODERN",
    // Отображать в творческом режиме
    // Стандартное значение: [false]
    creative: false
  },
  wailax: {
    // Энергия блоков
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "energy.enabled_block": true,
    // Энергию сущностей
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "energy.enabled_entity": true,
    // Черный список
    // Для настройки параметров откройте следующий файл
    // extra\energy_blacklist.json5
    "energy.blacklist": null,
    // Жидкости в блоках
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "fluid.enabled_block": true,
    // Жидкости в сущностях
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "fluid.enabled_entity": true,
    // Единицы измерения
    // Стандартное значение: [MILLIBUCKETS]
    // Доступные значения: [MILLIBUCKETS, DROPLETS]
    "fluid.display_unit": "MILLIBUCKETS",
    // Черный список
    // Для настройки параметров откройте следующий файл
    // extra\fluid_blacklist.json5
    "fluid.blacklist": null,
    // Содержание блоков
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "item.enabled_block": true,
    // Содержимое сущностей
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "item.enabled_entity": true,
    // Синхронизировать NBT данные
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "item.nbt": true,
    // Режим отображения
    // Сетка: отображение подобно интерфейсу контейнера
    // Список: отображение в виде списка
    // Динамически: до заданной высоты отображение в виде сетки, дальше, в виде списка
    // Стандартное значение: [DYNAMIC]
    // Доступные значения: [GRID, LIST, DYNAMIC]
    "item.display_mode": "DYNAMIC",
    // Максимальная высота
    // Стандартное значение: [3]
    "item.max_height": 3,
    // Сортировать по количеству
    // Стандартное значение: [true]
    "item.sort_by_count": true,
    // Масштабирование в режиме сетки
    // Стандартное значение: [1.0]
    "item.grid_mode_scale": 1.0,
    // Черный список
    // Для настройки параметров откройте следующий файл
    // extra\item_blacklist.json5
    "item.blacklist": null,
    // Процесс крафта блоков
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "progress.enabled_block": true,
    // Процесс крафта сущностей
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "progress.enabled_entity": true,
    // Показать оставшееся время
    // Требует установленного WTHIT на сервере, иначе, будет заблокировано на [false]
    // Стандартное значение: [true]
    "progress.time": true,
    // Заменить подсказку предмета
    // Заменяет подсказку, если рецепт имеет предметный вход и/или выход. Если отключено и отсуствует предметный вход и /или выход, отображается только полоса прогресса
    // Стандартное значение: [false]
    "progress.replace_items": false,
    // Высота полосы прогресса
    // Применимо только если используется полоса прогресса, а не стрелка
    // Стандартное значение: [1]
    "progress.height": 1,
    // Черный список
    // Для настройки параметров откройте следующий файл
    // extra\progress_blacklist.json5
    "progress.blacklist": null
  }
}